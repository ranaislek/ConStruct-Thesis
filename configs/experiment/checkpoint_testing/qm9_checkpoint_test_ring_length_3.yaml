# @package _global_
# Checkpoint Testing Configuration for Ring Length ≤ 3 Constraint

general:
  name: 'qm9_checkpoint_test_ring_length_3'
  wandb: 'disabled'
  samples_to_generate: 100
  sample_every_val: 1          # Sample every validation
  check_val_every_n_epochs: 1         # Check validation every epoch
  sample_every_val: 1          # Sample every validation
  check_val_every_n_epochs: 1         # Check validation every epoch
  samples_to_save: 50
  number_chain_steps: 200

model:
  # Exact dimensions from debug checkpoints
  diffusion_steps: 300
  n_layers: 4
  hidden_mlp_dims:
    X: 128
    E: 64
    y: 64
  hidden_dims:
    dx: 128
    de: 64
    dy: 64
    n_head: 4
    dim_ffX: 256
    dim_ffE: 128
    dim_ffy: 128
  rev_proj: ring_length_at_most
  max_ring_length: 3
  transition: absorbing_edges

dataset:
  datadir: data/qm9
  name: qm9
  remove_h: true
